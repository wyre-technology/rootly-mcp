name: rootly-mcp
region: nyc
services:
  - name: rootly-mcp
    github:
      repo: wyre-technology/rootly-mcp
      branch: main
      deploy_on_push: true
    dockerfile_path: Dockerfile
    http_port: 8080
    instance_count: 1
    instance_size_slug: basic-xxs
    routes:
      - path: /
    health_check:
      http_path: /health
      initial_delay_seconds: 10
      period_seconds: 30
      timeout_seconds: 5
    envs:
      - key: MCP_TRANSPORT
        value: http
      - key: ROOTLY_API_TOKEN
        type: SECRET
        scope: RUN_TIME
      - key: LOG_LEVEL
        value: info
